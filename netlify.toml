# Smart-Elixir React app lives in web/ (not the legacy Streamlit app.py at repo root).
# Netlify reads this file from the repository root automatically.

[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# Vite 8 needs modern Node; without this an older site-default Node makes
# every deploy fail and the site silently stays on the last good build.
[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# blob: is needed by the study module: imported documents are held as blobs
# (image preview, "export the original file") and the PDF text extractor runs in
# a worker. Everything still comes from this origin - no third-party sources.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Content-Security-Policy = "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self' blob: https://formsubmit.co; manifest-src 'self'; worker-src 'self' blob:; object-src 'none'; frame-ancestors 'self'; base-uri 'self'; form-action 'self'"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Content-Type = "application/javascript"
    Cache-Control = "no-cache"
