Staging area. Each file holds one finished topic as a standalone, typecheckable
module; `scripts/mergePending.mjs` folds them into their subject files and the
directory is deleted once the merge is verified. Nothing here ships.
