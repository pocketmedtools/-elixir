/**
 * Diagrams for fm-principles, keyed by topic id.
 *
 * Empty until written. A topic with no entry renders without a diagram, which
 * is the honest state — a diagram that misrepresents the material is worse than
 * none at all.
 */
import type { DiagramSet } from "../lib/types";

const diagrams: DiagramSet = {};

export default diagrams;
