__pycache__/
*.pyc
*.mp3
.streamlit/secrets.toml
hl_extract.txt
hl_mono.txt
ref_comp.txt
patch_pediatric_db.py
web/node_modules/
web/dist/
.env
.env.*
!.env.example
web/android/keystore/

# Generated review digests — regenerate with `npm run digest`
fmprep/review/**
!fmprep/review/README.md

# Generated single-file build — regenerate with `npm run single`
fmprep/dist-single/

# Throwaway helper scripts an agent writes while measuring a subject. They are
# named with a leading underscore so they never mix with the real checks in
# fmprep/scripts, and they are not worth committing or worth deleting out from
# under a running agent.
fmprep/scripts/_*.ts
